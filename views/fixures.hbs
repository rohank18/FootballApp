<!DOCTYPE html>
<html lang="en">
<head>
  <title>FootballWorld</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.14.0/css/all.min.css"/>
  <link href="css/custom.css" rel="stylesheet">
</head>
<body>
    {{> header}}
    <div class="container">
        <fieldset>
        <form class="form-inline" action="/fixures" method="get">
            <div class="form-group">
            <label for="league">League</label>
            <select name="leagueI">
                <option value="2014"> La Liga </option>
                <option value="2021"> English Premier League </option>
                <option value="2015"> Ligue 1 </option>
                <option value="2019"> Serie A </option>
                <option value="2002"> Bundesliga </option>
            </select>
            </div>
            <button type="submit" class="btn btn-default" value="Get Standings">Submit</button>
        </form>
            <table id="cumshot">
                {{#head}} 
                    <tr>
                    <th>Game Week</th>
                    <th>Home</th>
                    <th>Away</th>
                    <th>Date</th>
                    <th>Result</th>
                    </tr>
                {{/head}}
                {{#matches}}
                <tr>  
                    <td>{{matchday}}</td>
                    <td>{{homeTeam.name}}</td>
                    <td>{{awayTeam.name}}</td>
                    <td>{{utcDate}}</td>
                    <td><span>{{getasdf score.fullTime}}</span><span>-</span><span>{{score.fullTime.awayTeam}}</span> </td>
                </tr>
                {{/matches}}
            </table>
        </fieldset>
    </div>
    {{> footer}}  

    <script>
        console.log()
    </script>
</body>